{
  "name": "verdeai-v5-0-workshop-build",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "node scripts/smoke-test.mjs",
    "backend": "cd backend && npm install && npm start"
  }
}
