{
  "name": "verdeai-v3-5-workshop-build",
  "version": "3.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "node scripts/smoke-test.mjs",
    "backend": "cd backend && npm install && npm start"
  }
}
