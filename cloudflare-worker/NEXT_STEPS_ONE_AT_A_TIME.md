# Next steps — one at a time

Do not use PowerShell or create another Cloudflare API token.

The next owner action is to replace the existing repository's `cloudflare-worker` folder with this corrected folder. After that, commit and push with GitHub Desktop. Then connect that folder through Cloudflare Workers Builds.
