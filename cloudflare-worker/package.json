{
  "name": "verdeai-v9-2-2-render-pilot-worker",
  "version": "9.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node test/static-contract.test.mjs",
    "codes": "node scripts/generate-pilot-codes.mjs",
    "deploy": "wrangler deploy",
    "safe-lock": "wrangler deploy --config wrangler.safe-lock.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
