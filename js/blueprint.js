function renderBlueprint(f){document.getElementById('plan').innerHTML=[['This week',f.action],['Observe','Use the spot at least twice. Notice comfort, shade, privacy, and whether you naturally return to it.'],['This weekend','Make the test area feel intentional: chair, pot, light, or simple edge.'],['Next month','Keep what works. Remove what you ignored. Spend only after the test proves itself.'],['One year','Build the strongest idea into a permanent feature.']].map(x=>`<div class="step"><b>${x[0]}</b><br>${x[1]}</div>`).join('')}