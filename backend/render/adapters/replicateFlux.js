export async function renderWithReplicateFlux(_request) {
  throw new Error("Replicate / FLUX adapter scaffold only. Add server-side REPLICATE_API_TOKEN and implementation before enabling paid renders.");
}
