# VerdeAI v9.2.2 Node Reference Backend

This Express backend is retained only as a local reference and compatibility test scaffold. The approved production backend is the Cloudflare Worker in `../cloudflare-worker`.

Its default environment remains locked and it is not the deployment target for the v9.2.2 pilot.

```bash
npm install
npm test
npm start
```
