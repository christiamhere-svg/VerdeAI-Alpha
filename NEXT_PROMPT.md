# Exact next VerdeAI development prompt

Use this only after deciding the five owner-gate items. Replace the bracketed answers, then paste the entire prompt into a new VerdeAI development chat with the v9.2 ZIP attached.

---

Continue VerdeAI from the verified Build v9.2 secure-pilot preparation milestone.

My explicit owner approvals are:

- Rendering provider: [APPROVE OpenAI GPT Image 2 / DO NOT APPROVE]
- Backend host: [APPROVE Cloudflare Worker Paid / DO NOT APPROVE]
- Total pilot budget: [APPROVE US$10 total, US$5 VerdeAI reservation cap, US$0.15 planning ceiling per request, with first-render provider-billing verification / REPLACE]
- Invited tester limit: [APPROVE 10 testers, one render per session, two per IP per 24 hours / REPLACE]
- Retention/deletion policy: [APPROVE no VerdeAI server storage, with provider processing and documented default API retention disclosure / REPLACE]

Build VerdeAI v9.2.1 as the controlled owner-only activation and validation milestone.

Do not open the pilot to testers yet.

Requirements:

1. Deploy or finalise the approved Cloudflare Worker backend without exposing provider secrets.
2. Add the provider API key only as an encrypted Worker secret; never place it in frontend code, files or chat output.
3. Keep one-image-only enforcement, browser resize, metadata stripping, byte limits, calibration-aware prompt, explicit consent, rate limits, Durable Object tester/spend reservation, 120-second timeout, non-sensitive logs and no-VerdeAI-image-storage policy.
4. Keep real provider calls blocked until health, estimate, rejection, consent, oversize, rate-limit, budget-lock and fallback tests pass against the deployed Worker.
5. Enable exactly one owner-controlled real render only after those tests pass.
6. Compare the real provider image with the calibrated overlay and verify buildings, rooflines, doors, windows and access are preserved.
7. Record actual render time, actual provider cost/usage, moderation outcome, image dimensions, mobile behaviour and any structural hallucinations.
8. Turn the kill switch back on immediately after the single owner test unless the result is explicitly accepted.
9. Preserve every existing VerdeAI feature and keep the free calibrated overlay fully usable.
10. Return the v9.2.1 deployment ZIP, Worker package, deployment/test instructions, validation evidence, actual cost/time record, updated documents and the exact next prompt for the invited-tester pilot.

Work in Autopilot Build Mode. Do not ask small design questions. Stop and ask only if an approved owner-gate value is missing or a real provider charge would exceed the approved limit.
